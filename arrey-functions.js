//const users =[{
  //  name:"mehmet",
    //age:29,
//},{
  //  name:"mehmet",
    //age:18,
//},{
  //  name:"sevilay",
   // age:40,
//}];


/* 
   push
   map
   find
   filter
   some
   every
   includes
   */
   //push
 //  users.push("ayşe");
   //users.push("fatma");

   //console.log(users);

   //map

//users.map((item) => {
  //  console.log(item);
//}) ;

//find

//const result = users.find((item) => item.name === "mehmet" && item.age > 20 );
//console.log(result);

//filter
//const filtered = users.filter((item) => item.name === "mehmet" && item.age < 20) ;
//console.log(filtered);


//some

//const some = users.some(item => item.age === 18);
//console.log(some);

//every
//const every= users.every((item) => item.age > 20 );
//console.log(every);

// inculudes

const meyveler = [ "elme", "armut", "muz"];
const mehmet = meyveler.includes("muz");
console.log(mehmet);
